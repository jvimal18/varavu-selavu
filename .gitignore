# Nuxt
.nuxt
.output
.data
.cache
build
.env
.env.local
*.log

# Node
node_modules
.pnpm-store

# Database (local dev + prod backups)
# Catches: data/dev.db, budget-backup.db, *.db-wal, *.db-shm, *.db.bak-*
data/
*.db
*.db-journal
*.db-wal
*.db-shm
# Safety backups of any DB before a migration (rename-on-deploy workflow)
*.bak-*
# WAL-mode files for prod backups (redundant with the .db-wal pattern but
# explicit so future-you doesn't get a surprise on git status)
budget-backup.db-wal
budget-backup.db-shm
budget-backup.db-journal

# Exports
exports/

# Deploy secrets (copy scripts/env.example to scripts/.env)
scripts/.env

# IDE
.vscode
.idea
.DS_Store

# Tooling (opencode/claude)
.opencode/
.agents/
.claude/
.slim/deepwork/

# Drizzle migrations ARE tracked

# Project notes (kept local; not tracked in remote or tags)
DECISIONS.md
