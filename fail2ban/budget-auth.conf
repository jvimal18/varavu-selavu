[Definition]
# Matches the auth-fail lines emitted by server/utils/rateLimit.ts
# (logged via console.error → systemd journal for the budget-tracker unit).
failregex = ^.*\[auth-fail\] ip=<HOST> user=<F-USER> reason=.*$
ignoreregex =
